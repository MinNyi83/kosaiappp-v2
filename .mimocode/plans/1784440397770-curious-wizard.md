# Dark/Light Mode Theme Support - All Public Pages

## Goal
Add dark/light mode toggle to all 6 HTML pages in `public/` that are currently dark-only, using the exact same theme system already working in `public/admin.html`. No feature/logic changes.

## Reference: admin.html Theme System (already working)

### CSS Variables
```css
:root, [data-theme="dark"] {
  --bg-body: radial-gradient(circle at top right, #111115, #070709);
  --bg-surface: #0c0c10;
  --bg-surface-alt: #09090b;
  --bg-card: linear-gradient(135deg, rgba(20, 20, 28, 0.7) 0%, rgba(12, 12, 18, 0.8) 100%);
  --bg-input: rgba(0, 0, 0, 0.3);
  --bg-overlay: rgba(0, 0, 0, 0.6);
  --border-subtle: rgba(255, 255, 255, 0.05);
  --border-medium: rgba(255, 255, 255, 0.08);
  --text-primary: #ffffff;
  --text-secondary: #e2e8f0;
  --text-muted: #94a3b8;
  --text-dim: #64748b;
}
[data-theme="light"] {
  --bg-body: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
  --bg-surface: #ffffff;
  --bg-surface-alt: #f1f5f9;
  --bg-card: linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(248,250,252,0.95) 100%);
  --bg-input: rgba(241, 245, 249, 0.8);
  --bg-overlay: rgba(0, 0, 0, 0.4);
  --border-subtle: rgba(0, 0, 0, 0.06);
  --border-medium: rgba(0, 0, 0, 0.1);
  --text-primary: #0f172a;
  --text-secondary: #1e293b;
  --text-muted: #475569;
  --text-dim: #94a3b8;
}
```

### Toggle Button SVG + Script
```html
<button id="theme-toggle" onclick="toggleTheme()" class="p-2 rounded-lg transition-all hover:bg-white/5" title="Toggle Dark/Light Mode">
  <svg id="theme-icon-dark" class="w-4 h-4 text-amber-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
  </svg>
  <svg id="theme-icon-light" class="w-4 h-4 text-amber-500 hidden" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
  </svg>
</button>
```

```js
(function() {
  const savedTheme = localStorage.getItem('am-theme') || 'dark';
  document.documentElement.setAttribute('data-theme', savedTheme);
  updateThemeIcons(savedTheme);
  function updateThemeIcons(theme) {
    const darkIcon = document.getElementById('theme-icon-dark');
    const lightIcon = document.getElementById('theme-icon-light');
    if (darkIcon && lightIcon) {
      if (theme === 'light') { darkIcon.classList.add('hidden'); lightIcon.classList.remove('hidden'); }
      else { darkIcon.classList.remove('hidden'); lightIcon.classList.add('hidden'); }
    }
  }
  window.toggleTheme = function() {
    const current = document.documentElement.getAttribute('data-theme');
    const next = current === 'dark' ? 'light' : 'dark';
    document.documentElement.setAttribute('data-theme', next);
    localStorage.setItem('am-theme', next);
    updateThemeIcons(next);
  };
})();
```

---

## Files to Modify (6 pages)

### 1. `public/index.html` (Landing Page)
- **Add CSS variables** at top of `<style>` (dark defaults + light overrides)
- **Update body** to use `var(--bg-body)` and `color: var(--text-secondary)`
- **Update `.glass-panel`** to use `var(--bg-card)`, `var(--border-subtle)`
- **Add theme-aware inputs** CSS block
- **Add toggle button** in desktop `<nav>` after "Staff Console" link
- **Add toggle script** before `</body>`
- **Key text colors** to update: headings (`text-white` → `var(--text-primary)`), paragraph text (`text-slate-400` → `var(--text-muted)`), footer, header

### 2. `public/app.html` (Technician Portal)
- **Add CSS variables** at top of `<style>`
- **Update body, .glass-panel, .input-dark** to use variables
- **Add toggle button** in header bar (next to refresh button)
- **Add toggle script** before `</body>`
- Keep indigo accent colors as-is (works in both modes)

### 3. `public/portal.html` (Client Portal)
- **Add CSS variables** at top of `<style>`
- **Update body, .glass-panel, .input-dark** to use variables
- **Add toggle button** in header next to "Pull Logs" button
- **Add toggle script** before `</body>`

### 4. `public/contact.html` (Contact Page)
- **Add CSS variables** at top of `<style>`
- **Update body, .glass-panel, .contact-card, .form-input** to use variables
- **Add theme-aware Leaflet map** CSS (remove `filter: invert()` in light mode)
- **Add toggle button** in desktop `<nav>` (before hamburger, after "Staff Console")
- **Add toggle script** before `</body>`

### 5. `public/portfolio.html` (Portfolio Page)
- **Add CSS variables** at top of `<style>`
- **Update body, .glass-panel, .contact-card** to use variables
- **Add toggle button** in header nav
- **Add toggle script** before `</body>`

### 6. `public/jobs.html` (Jobs Dashboard)
- **Add CSS variables** at top of `<style>`
- **Update body, .glass-panel** to use variables
- **Add toggle button** in header nav
- **Add toggle script** before `</body>`

---

## What NOT to change
- No JavaScript business logic (API calls, form handlers, navigation)
- No feature behavior (modals, filters, maps, checklists)
- Accent colors (amber, indigo, emerald, rose) stay as-is
- Only CSS variables, toggle button HTML, and toggle script additions

## Verification
1. Open each page in browser
2. Click toggle button → page switches to light mode
3. Reload page → theme persists from localStorage
4. Navigate to another page → theme persists across pages
5. Verify all forms, modals, and interactive features still work
6. Check Leaflet map in contact.html adapts (dark filter removed in light mode)
